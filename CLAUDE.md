# CLAUDE.md — tanuki-spec Claude Code運用ガイド

このリポジトリは、要件定義・設計・テスト・画面モック・レビューを支援するエージェントスキル集である。
Claude Codeは、テンプレートを埋めることではなく、案件固有の理解と判断を深め、実装へ安全に引き渡せる仕様を作るために使う。

## 1. 基本方針

- 出力とレビューコメントは日本語で書く。
- 専門用語は、開発初心者にも判断できる言葉へ言い換える。
- 根拠がない内容を推測で確定せず、`[要確認: 質問]`として残す。
- 項目数や文書量を品質とみなさない。利用者影響・不確実性・結合・復旧難度に応じて深さを変える。
- 人が読む本文は理解順に編集し、監査用のID・根拠・対応表は付録またはYAMLへ分ける。
- 調査やレビューではサブエージェントを使い、生成担当とレビュー担当を可能な限り分離する。

## 2. 読む順序

作業開始時は、必要な範囲に絞って次の順序で読む。

1. この`CLAUDE.md`
2. [`AGENTS.md`](./AGENTS.md)の共通規約とドキュメント同期ルール
3. 対象スキルの`skills/<skill-name>/SKILL.md`
4. 対象スキルから直接参照される共有リファレンス
5. 対象案件に`docs/spec/_project/profile.md`がある場合は、その案件固有ルール

全体フローは[`FLOW.md`](./FLOW.md)、スキル一覧は[`SKILLS.md`](./SKILLS.md)、起動入力は[`TEMPLATES.md`](./TEMPLATES.md)を正本とする。既に書かれている内容を別文書へ複製しない。

## 3. Claude Codeの役割

Claude Codeは主に次を担当する。

- プロジェクト文脈と既存実装の調査
- 要件の曖昧さ、例外、失敗、復旧、状態遷移の深掘り
- 複数の設計案とトレードオフの比較
- 案件の既存デザインを踏まえた画面モックの設計
- Codexが実装した変更の独立レビュー
- 仕様変更を正本へ差し戻す判断

tanuki-spec自体のPython・テンプレート・スキルを変更するときは、Codexが実装し、Claude Codeは要件との整合と回帰リスクを独立してレビューする。レビュー依頼でコード修正まで明示されていない場合、Claude Codeはファイルを変更せず指摘だけを返す。

## 4. 正本と派生成果物

### tanuki-specリポジトリ

- `tanuki-spec-all/`は共有コアの唯一の正本である。
- 各スキルの共有スクリプト・テンプレート・リファレンスはsymlinkで参照する。
- symlink先の内容を変更するときは共有コアを編集し、symlinkを通常ファイルへ置き換えない。
- 生成テンプレートは手編集せず、正本を直して`evaluation/generate_templates.py`で再生成する。

### tanuki-specを使う対象プロジェクト

- `docs/spec/`を要件・設計・テストの唯一の正本とする。
- `views/`のHTMLは閲覧用の派生成果物であり、直接編集しない。
- 人向けHTMLは原則として、サマリ・要件・一枚設計書・テストケース・画面モックだけにする。
- `.kiro/specs/`はcc-sddの実装運用領域であり、tanuki-specの本文を複製しない。
- 仕様変更は`.kiro/`だけで完結させず、`docs/spec/`へ戻してゲート通過後に再連携する。

## 5. cc-sddとの境界

cc-sddとの役割分担と安全条件は[`tanuki-spec-all/references/cc-sdd-integration.md`](./tanuki-spec-all/references/cc-sdd-integration.md)を正本とする。

- cc-sddはリポジトリへ複製せず、`integrations/cc-sdd/compatibility.json`で固定した公式npm版を使う。
- 完全未導入の場合は固定版と追加先を提示し、ユーザーの明示同意後だけ公式インストーラーで追加する。
- 旧版・部分導入・既存ファイル競合・symlink競合を検出した場合は自動上書きせず停止する。
- preflightの互換性判定は、バージョン番号だけでなく必須Skillとartifactの実体まで確認する。
- bridgeはDoD通過後だけ実行し、数値要件IDと正本リンクを持つ薄い参照カードを生成する。
- bridgeは手書きファイル、symlink、既存の`tasks.md`を上書きしない。
- `kiro-spec-tasks`には参照カードの要約だけでなく、リンクされたtanuki正本を読ませる。
- 外部版で互換性と安全性を維持できない事実が確認された場合に限り、source内包を再検討する。

## 6. 仕様・画面品質

生成判断は[`tanuki-spec-all/references/spec-quality-principles.md`](./tanuki-spec-all/references/spec-quality-principles.md)に従う。

- テンプレートは監査漏れを防ぐ器であり、人向け本文の固定章立てではない。
- 高リスク要件は、正常系だけでなく例外・境界値・権限・失敗時・復旧・観測方法まで掘る。
- 設計判断には、採用案、比較した代替案、採用理由、捨てた利点、変更条件を残す。
- 画面モックは対象プロジェクトの既存画面・ブランド・主要導線を調査してから作る。
- 高リスクなUI判断は複数案を比較し、既存パターンを継承する場合も適合理由を記録する。
- 機械ゲートの合格と、人が理解・判断できる品質を別々に確認する。

## 7. 変更時の同期ルール

スキルの追加・変更・削除では、[`AGENTS.md`](./AGENTS.md)の「ドキュメント同期」に従う。
特に次の不整合を残さない。

- `SKILL.md`と`TEMPLATES.md`の起動テンプレート
- スキル実体と`README.md`・`SKILLS.md`・`AGENTS.md`の一覧
- 工程番号と`FLOW.md`
- 共有コアと各スキルのsymlink
- 互換性台帳とpreflightの期待値
- 正本Markdown/YAMLと閲覧用HTML

## 8. 検証

変更範囲に応じて、少なくとも次を実行する。

```bash
# ルート文書とスキル実体の同期
python3 -m unittest discover -s tests -v

# 共有コアの全決定論チェック
cd tanuki-spec-all
.venv/bin/python evaluation/run_harness.py

# 差分の空白・競合マーカー確認
git diff --check
```

cc-sdd連携を変更した場合は、隔離した一時ディレクトリで実際の公式CLIを使い、Codex版とClaude Code版の両方を確認する。既存プロジェクトに対しては、未導入・現行版・旧版・部分導入・競合・symlinkの各状態で、意図しない上書きがないことを検証する。

画面モックを変更した場合は、YAMLゲートとHTML構造検証に加え、実際にブラウザで主要画面と遷移を目視確認する。

## 9. PRレビュー手順

レビューでは最新コミットだけでなく、PRのbaseからheadまでの差分全体を読む。指摘は重要度順に提示し、各指摘に次を含める。

- 重大度（Blocker / High / Medium / Low）
- ファイルと行番号
- 何が壊れるか、どの条件で再現するか
- 根拠となる仕様・契約・テスト
- 最小限の修正方針

次を優先して確認する。

1. データ破壊、既存ファイル上書き、symlink追従、コマンド注入などの安全性
2. preflightの誤判定、バージョン互換性、外部CLIの実挙動
3. 正本と派生成果物の逆転、cc-sddとの責任境界違反
4. 冪等性、途中失敗後の再実行、部分更新
5. スキル・テンプレート・文書・symlinkの同期漏れ
6. 正常系だけを通しているテスト、実装を再現しているだけの弱いテスト
7. 案件固有の深掘り・読みやすさ・UI適合性の後退

問題がない場合も、確認した差分・実行した検証・残る未検証リスクを明記する。「問題なさそう」の一文だけで承認しない。

## 10. Git・機密情報

- ユーザーの既存変更を勝手に戻さない。
- `git reset --hard`、広範囲な削除、履歴改変を行わない。
- APIキーやトークンをコード・設定・レビューコメントへ書かない。
- 外部依存の取得元、版、ライセンスを互換性台帳で確認する。
- コミットメッセージは1行、日本語、Gitmoji付きにする。
